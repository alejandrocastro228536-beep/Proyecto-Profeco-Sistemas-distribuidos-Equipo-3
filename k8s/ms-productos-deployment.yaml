apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-productos
  labels:
    io.kompose.service: ms-productos
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: ms-productos
  template:
    metadata:
      labels:
        io.kompose.service: ms-productos
    spec:
      containers:
        - name: ms-productos
          image: ms-productos
          imagePullPolicy: Never
          ports:
            - containerPort: 8081
          env:
            - name: QUARKUS_HTTP_PORT
              value: "8081"
            - name: QUARKUS_DATASOURCE_DB_KIND
              value: postgresql
            - name: QUARKUS_DATASOURCE_JDBC_URL
              value: jdbc:postgresql://postgres:5432/productosdb
            - name: QUARKUS_DATASOURCE_USERNAME
              value: postgres
            - name: QUARKUS_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: profeco-db
                  key: password
            - name: QUARKUS_HIBERNATE_ORM_DATABASE_GENERATION
              value: update
            - name: RABBITMQ_HOST
              value: rabbitmq
            - name: RABBITMQ_PORT
              value: "5672"
            - name: RABBITMQ_USERNAME
              value: guest
            - name: RABBITMQ_PASSWORD
              value: guest
